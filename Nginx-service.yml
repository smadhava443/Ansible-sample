---
- name: Install and restart Nginx
  hosts: all
  become : yes
  become_user: root
  tasks:

    - name: Install nginx
      apt:
        name: nginx
        state: present
    - name: stop nginx
      service:
        name: nginx
        state: stopped
